import { GameExtensionInfo } from 'src/types';
import RootStore from 'src/stores/RootStore';

/**
 * Gen3 todos
 * - implement roll augment rule
 * - test elite 4 space
 */

 export default (rootStore: RootStore): GameExtensionInfo => {
   return {
     gameEvents: {},
   };
 };